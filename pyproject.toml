[project]
name = "data-viz-dashboard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "streamlit[charts]>=1.56.0",
]

[dependency-groups]
dev = [
    "watchdog>=6.0.0",
]
